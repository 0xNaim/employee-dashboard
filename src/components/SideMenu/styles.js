import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  sideBar: {
    display: "flex",
    flexDirection: "column",
    position: "absolute",
    left: 0,
    width: 320,
    height: "100%",
    background: "#253053",
  },
}));
